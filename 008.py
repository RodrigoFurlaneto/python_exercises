meters = float(input('Enter the value in meters: '))

centimeters = meters * 100
millimeters = meters * 1000

print('{} meters is equal {} centimeters and {} millimeters'.format(meters, centimeters, millimeters))
